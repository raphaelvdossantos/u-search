import styled from "styled-components";

export const Btn = styled.button`
    background: none;
	color: inherit;
	border: none;
	padding: 0;
	outline: inherit;
    cursor: pointer;

    margin: 0 0.25rem ;
    `